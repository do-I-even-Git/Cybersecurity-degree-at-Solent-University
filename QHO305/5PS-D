#include <stdio.h>

int main() {

int daysNumber = 0;
int daysArray[daysNumber];

do{
    printf("How many days would you like to input today? :)\n");
scanf(" %d", &daysNumber);
if (daysNumber > 30 || daysNumber < 1)
{
    printf("-----------ERROR-----------\nDay count needs to be an integer value between 1 and 30.\n Please try again.\n\n\n");
} else{
printf("We have %d days here.", daysNumber);}
    
}
while (daysNumber > 30 || daysNumber < 1);







    return 0;
}
